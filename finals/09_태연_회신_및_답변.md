# 09 — 태연 회신(07-15 22:56) 요지 + 진산 답변

> 원문: Slack DM `태연_회신_0715.md` (F0BJC9E422U). 발표자료 원본(pptx/pdf)·슬랙로그 백업은 별도 보관.

## 태연 검토 7건 → 답변/처리

| # | 태연 지적 | 판정 | 처리 |
|---|---|---|---|
| 🔴A1 | 최종점수 표기 혼재 + 1위 점수 미확인 | **정당** | **최종 리더보드 확정(07-15 진산 스크린샷): 1위 0.79878 → 격차 0.00112.** (앞서 답한 0.79862/0.00095는 마감 전 스냅샷 — 정정) 표기 규칙: 본문 0.7977 / 재현·제출 문서만 0.7976673203 |
| 🔴A2 | 준현 블로커 3개 문서 미해결, 목요일 회신은 빠듯 | **정당** | 준현에게 "손실식+CLI 한 줄 수요일 선회신" 재촉 (팀채널) |
| 🟡A3 | amw4 vs amhyb 동점 정체 + 최종 선택 확인 | **✅ 해소(07-15)** | **Dacon 최종 선택본 = `rfinal_amhyb_m10.zip`** (진산 확인). 추론 7:15로 동점팩 중 더 빠름 = 본선 속도평가에도 유리한 선택. 남은 것: amhyb "트리오 변경"의 정확한 구성 = 준현 명세 대기 |
| 🟡A4 | seed7070 학습 로그 확보 여부 | 부분 해소 | trioT 계열(s909/s7070) 로그는 진산 보유 (A100 kd_s909.log / 4070 kd_s7070.log). **단 최종 amw4-7070은 준현 Colab lane이라 그의 로그 필요** → A2에 묶음 |
| 🟡A5 | HyperCLOVAX 라이선스 상업조항 미확정 | 정당 | **진산이 라이선스 원문 확인 → 05 §4 확정** (액션) |
| 🟢A6 | 슬라이드 수 표기 (13 vs 14) | 정당 | 실물 pptx = **14장** 확인됨 → 14장 기준 통일, `06` 구성안은 참고용으로 강등 |
| 🟢A7 | 강건성 probe의 인코더 레인 맥락 누락 | 정당 | `03` §3.5에 맥락 한 줄 추가 + 슬라이드 13 태연 표기에 "강건성 검증" 포함 |

## 태연 파트 3건 — 전부 채택

- **B-1 추론 파이프라인 1페이지**: 채택 (슬라이드 5 소스). script.py 실물 대조는 ① 팀채널의 `rfinal_mgn125.zip` script.py로 구조(라우팅·룰 12종) 90% 대조 가능, ② 최종본과의 차이(margin 1.0 + Weak4-AM 게이트)는 준현 명세로 확정(07-16). 단계별 perf_counter 로그는 없음 → "forward ~95%, 멤버 통과 34.1%" 수준으로 발표 (준현 실측 — 종전 24% 추정치 폐기).
- **B-2 제출 오류 관리 문단**: 채택 — 슬라이드 11 + QnA 13번 답변으로.
- **B-3 포스터 모니터링**: 태연 확정, 진산 공지 모니터링과 이중화.

## C 역할표기 — 태연 제안 기반 합의안 (팀채널 상정)

- 베이스 = QnA 15번 4인 분담
- focal = 팀 표준 (특정인 크레딧 없음)
- R1 계열: **발굴 = 원주(EDA budget-zone) / 검증 = 진산(5-fold 게이트 하니스)** 분리 표기
- 태연 = 추론 파이프라인·제출 관리·**직렬화 강건성 검증** (인코더 레인 탐색 맥락 QnA 대비)
- 준현 = 챔피언 레시피·KD/sieve·최종 amw4 lane

## 태연 위임 확정

1. A1 표기 통일안 → 태연이 00~06 일괄 수정 문안 작성, 진산 검수
2. script.py 대조 → mgn125 우선, amw4 준현 대기
3. 포스터 모니터링 ON
