# 08 — 원주 EDA 3장면 문안 (07-15 22:50 DM 회신 원문 보관)

> 슬라이드 2·7·10용. 각 장면 = 그림 1장 매칭 (fig1~3, PNG 200dpi — **07-15 기준 미착, 원주가 DM 업로드 예정**).
> 원본 md·그림 생성 스크립트는 원주 보유 — 문구/색 수정 요청 즉시 반영 가능. **리허설 07-19(일) 저녁 원주 가능.**

---

## 장면 ① 클래스 분포/불균형 (fig1)

70,000행에서 탐색4(read/grep/list/glob)는 전체의 **41.1%**를 차지하는 다수 클래스인데도 champion per-class F1은 0.52~0.68로 14클래스 최하위 구간입니다. 반대로 편집 계열은 0.98~1.00으로 사실상 풀린 상태 — Macro-F1의 남은 질량은 "희소 클래스" 문제가 아니라 *다수인데도 못 푸는 탐색4*에 집중돼 있고, 이것이 조건부α·action-margin·룰 스택이 탐색4에 3층 개입한 이유입니다.

## 장면 ② 탐색4 aleatoric 증명 (fig2: 4×4 혼동 히트맵)

champion 오답의 **71.8%**가 탐색 4×4 안에서 발생하는데, 모델 결함이 아니라 데이터 성질임을 5개 독립 증거로 확정: ① 동일 컨텍스트 라벨 93% 갈림 ② 인간 재라벨 일치 21% ③ TTA flip 0/500 ④ CMI≈0 ⑤ Bayes 상한 ≈0.570 (champion은 이미 상한 부근). 추가로 이 천장의 원천이 태스크 본질이 아니라 **sim 생성기**임을 sim/au 분리로 특정(au에서는 탐색4 0.889), 라벨 무결성은 세션 체인 44,426행 전수 대조 **오류 0건 100.00%**로 증명.

**절단 감사 요약 (장면② 보조)**: 절단율 0.00%(1/70,000, 그 행도 정답), p50=207/p99=315/max=386 — 캡 미접촉. 짧은 레코드가 오히려 고오류(Q1 0.320 vs Q4 0.167) = 정보 부족이 원인, "길어서 잘림" 피해 구조적 부재. → "입력 손실 없음 + 남은 오류는 라벨 다중정답성" 서사로 연결.

## 장면 ③ budget-zone 발견 (fig3: 구간별 오류율 막대)

budget 양극단 대칭 고오류(<2.5k 0.424 / 2.5-5k 0.390 / 190k+ 0.320 vs 평균 0.216), 회수 가능한 건 하위 구간뿐. 원인 = 토크나이저가 숫자는 보지만 *임계 비교(<5000)를 학습 못 하는* 구조적 사각지대 — wrap-up 정책이 텍스트에 표현되지 않는 유일한 연속값 규칙. 이 발견이 R1(Public +0.00108 실증)~룰 10종 스택(+0.0015~0.0029, 5폴드×2시드 전수, 충돌 0)의 기원.

## Canvas 그림 후보 4개 (원주 선별)

1. budget 양극단 대칭 막대 → fig3으로 제작 완료
2. harm 3분해 도식 (슬라이드 8 또는 10)
3. **세션 체인 라벨 무결성 도식** (id 구조 → 44,426/44,426 → "히든=세션당 1행" 반증까지) — 데이터분석 킬러 슬라이드로 강추
4. 시드=순수 노이즈 (17모델 2k↔20k 순위 재배열) — 슬라이드 10 근거

(슬라이드 4 여정 그래프·슬라이드 6 산점도는 진산 제작분이라 중복 제외)

---

# 검증 결과 (진산 대조, 07-15)

## ✅ 정합 확인 (내 기록과 일치)

| 원주 수치 | 대조 결과 |
|---|---|
| R1 Public +0.00108 | ✅ 정확 (0.7942572386→0.7953413482 = +0.0010841) |
| 룰 스택 +0.0015~0.0029, 5폴드×2시드, 충돌 0 | ✅ 03 문서와 일치 |
| 인간 재라벨 일치 21% | ✅ 준현 248행 재라벨 기록과 일치 |
| Bayes 0.570 / 93% 갈림 / TTA 0/500 / CMI≈0 | ✅ 전부 기존 실측 기록과 일치 |
| au 탐색4 0.889 / 세션체인 44,426 오류 0 | ✅ 원주 07-14 분석과 일치 |
| 절단율 0.00% (1/70,000), p50/99/max=207/315/386 | ✅ 트랙 A 감사와 일치 |

## ✅ 확인 요청 3건 — 전부 해소 (원주 판정 07-16 15:38)

1. **오답 비중 → 71.8% ("약 72%")로 전 문서·장표 통일 확정.** 원주가 champ_oof 70k 전수 재측정: "true·pred 모두 탐색4(4×4)" = **71.8%** ← 표준. 가능한 5종 확장 정의를 전부 재측정해도(71.9~72.6%) **60%는 어떤 정의로도 재현 불가** — 구세대 모델(Qwen 라인) 아티팩트로 추정, 재현 불가 수치는 코드검증·QnA에서 위험하므로 폐기. 정의 각주: "오답 중 정답·예측이 모두 탐색4인 비율, champion OOF 70k". → 태연 슬라이드 7 수정 확정.
2. **"41%" 이중등장 → 그림에만 남기고 본문에서 제거.** fig1 제목이 숫자를 말하게 하고, 같은 장표 본문·발표 멘트는 "데이터의 5분의 2를 차지하는 다수 클래스"로 풀어쓰기. 타 장표 재인용 시 41% 표기 1회만 허용. 41.1%(= 28,782/70,000 — 준현 AM 적용 행수와 동일 값, 교차 일관) 수치 자체는 재검증 완료. → 태연 슬라이드 2 표현 조정 확정.
3. ~~**fig3 "190k+" 구간**~~ → **✅ 해소 (07-15 진산 직접 검증)**: 4070 train.jsonl 70k 전수 스캔 결과 max budget = **199,741**, ≥190k 419행(0.60%), ≥25k 89.1% — **190k+ 실존, 원주가 맞음.** (기존 "2k~25k 중심" 인식은 룰 임계 구간 한정이었음.)

## 💡 표현 조정 제안 (마이너)

- 장면② 증거⑤ "champion은 이미 상한 부근": champion 0.58 > 상한 추정 0.570이라 "상한을 넘었다"는 QnA 공격 가능 → **"추정 상한과 통계적으로 동일 수준(±추정오차 내)"** 로 완화 권장.
- 장면① "편집 계열 0.98~1.00 사실상 풀린 상태": R1h가 apply_patch→edit 83건을 고친 사실과 공존 가능(0.36% 수준)하나, "풀린 상태" 단정보다 "F1 0.98+"의 수치 제시가 안전.
