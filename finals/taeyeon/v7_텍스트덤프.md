<!-- Slide number: 1 -->
2026 AI·SW 중심대학 디지털 경진 대회
AI Agent 행동 의사결정 예측 챌린지
토큰강도

임준현 · 노진산 · 목원주 · 김태연

2026.08.11
Powered by HyperCLOVA X

<!-- Slide number: 2 -->
우리가 잡은 포인트
정답을 맞히는 문제가 아니라, 생성기의 행동 습관을 재현하는 문제
라벨을 고치는 대신 학습 압력을 완화한다 → 0.7852 → 0.7977 (+0.0125)
무엇을 발견했나
WHY  ·  데이터

라벨은 최적 행동이 아니라
생성기가 실제 고른 행동

LLM 2종 상호 일치 79.8%
라벨과의 일치 21%

→ 모델 편향이 아닌
→ 라벨 자체의 성질
그래서 무엇을 했나
HOW  ·  알고리즘

텍스트로는 도달 불가 (CMI?0)
라벨 교정도 실패

고치지 않고 압력을 낮춘다
→ KD · sieve · 조건부 α

남은 제약은 10분 추론
→ 0.5B 3개 · 저마진 라우팅
얼마나 올랐나
RESULT  ·  검증

HCX-0.5B 단일   0.7852
KD 4단계          +0.0125

최종 Macro-F1  0.7977
베이스라인 대비 +0.362

→ OOF↔Public ±0.002
→ 모든 경로에 인과 근거
28

<!-- Slide number: 3 -->
01. 우리가 푼 문제
탐색 4개 클래스가 최다인데 F1은 최하위 ? 여기서 점수가 결정된다
read · grep · list · glob 은 요청 문구가 겹쳐 하나를 올리면 다른 하나가 내려간다
4개 클래스(read · grep · list · glob)
→ 요청 문구와 경로 공통으로 등장

정답은 최적 행동이 아님
→ 실제 선택한 행동

하나를 개선하면 다른 클래스가 하락

![](그림8.jpg)
7

<!-- Slide number: 4 -->
02. 라벨을 다시 읽었다
라벨은 의미상 정답이 아니라, 생성기가 실제 고른 행동의 기록
LLM 판단과의 일치 list 5% · glob 15% → 정답 학습이 아니라 습관 재현 문제

![](그림17.jpg)
라벨 = 습관적 행동 기록
→ 의미적 정답 아님

요청 ↔ 라벨 불일치
→ 경로·패턴이 요청과 무관

전역적 행동 습관
→ 오류행 한정 아님
9

<!-- Slide number: 5 -->
03. 편향인가, 성질인가
모델 2종이 서로는 79.8% 일치하는데 라벨과는 21% ? 특정 모델의 편향이 아니다
정답 행에서도 불일치 → 오류행 한정이 아닌 전역적 행동 습관 (실제 세션은 88.2% 일치)

모델이 정답을 맞힌 행에서도
list · glob 일치 13~23%

→ 오류 행만의 문제가 아님
→ 전역적 행동 습관

※ 발산은 시뮬레이션 세션에 한정

※ 실제 세션은 라벨과 88.2% 일치

데이터셋 라벨
21.0%
23.4%
모델 간 일치 79.8%
GPT-5.6
Sonnet
10

<!-- Slide number: 6 -->
04. 두 가설을 모두 기각했다
단서를 더 넣어도, 라벨을 고쳐도 벽을 못 넘었다
→ 그래서 라벨을 고치는 대신 학습 압력을 완화하는 쪽으로 방향을 틀었다
가설 ① 텍스트·구조의 추가 단서

구조 단서의 조건부 상호정보량 CMI ? 0

입력을 바꿔도 예측 불변 TTA flip 0/500행
가설 ②  라벨 교정

5개 모델 만장일치 답도 LLM 판단과 24%만 일치
→ 틀린 라벨이 아니라 롱테일 패턴
기각
기각

라벨은 고칠 대상이 아니라, 학습 압력을 완화할 대상
11

<!-- Slide number: 7 -->
05. 제안 시스템 전체 구성
10분 안에 끝내야 한다 ? 그래서 불확실한 행만 앙상블로 보냈다
0.5B 3개 · 저마진 34.1%만 라우팅 → 속도와 점수를 동시에 확보 (7:15 / 10:00)
입력

test.jsonl 30,000행

직렬화 · len384

출력

submission
.csv

Main

amw4 · M8
교사 KD + Weak4 AM

INT8 · 568MB
저마진 앙상블

확신 낮은 34.1%만
멤버 2개 추가

model_b · c (INT4)
룰 보정

budget 임계 12룰

OOF 교차검증 통과분

불확실한 행만 앙상블로 보내 속도와 점수를 동시에 확보
13

<!-- Slide number: 8 -->
06. 모델 크기는 제약이 결정했다
큰 모델이 아니라, 시간 예산 안에서 가장 강한 모델
Qwen3-0.6B 0.781 → HCX-0.5B 0.7852 : 한국어 사전학습이 만든 +0.004

모델 선정 경로
인코더(mDeBERTa) → 디코더 전환

Qwen3-0.6B  0.781  →  HCX-0.5B  0.7852

→ 한국어 사전학습이 만든 +0.004

→ 직렬화 current_v1 · 최대 길이 384

→ int8 양자화로 1GB 제약 충족

파라미터 0.5B 선정 근거

추론 시간: 실제 토큰 수에 비례
동적 패딩 → 길이 캡 축소는 시간 절감 0

→ 단독 ≤ 0.6B급만 10분 통과

→ 0.8B 3-epoch은 실측 시간 초과

큰 모델은 교사로만 사용

14

<!-- Slide number: 9 -->
07. 지식 증류 ? 핵심 알고리즘
좋은 교사는 큰 모델이 아니라, 학생이 모르는 걸 아는 모델
암기율이 높으면 가르칠 이견이 없어 증류 이득이 사라진다 → 0.7852 → 0.7977 (+0.0125)

![](그림17.jpg)
① KD (M8 교사, α0.5 · T3)   +0.0039

② 조건부 α (Weak4=0.7)     +0.0005

③ consensus sieve            +0.0043

④ Weak4 action-margin KD

0.7852 → 0.7977
15

<!-- Slide number: 10 -->
08. 규칙 기반 보정
숫자 크기 비교(budget<5000)는 텍스트로 학습이 안 된다 → 결정적 룰로 보정
12개 룰 전부 OOF 교차검증 통과분만 채택 ? 기각한 룰이 채택한 룰보다 많다

![](그림11.jpg)
16

<!-- Slide number: 11 -->
09. 저마진 앙상블
전 행 3모델은 시간 초과 ? 확신 없는 행만 멤버를 추가했다
margin<1.0 인 34.1%만 통과 → 나머지 66%는 단일 모델 · 최종 0.7977
main 전 행 추론
HCX-0.5B · INT8
margin<1.0
저마진 34.1%만 멤버 추가
model_b · c (INT4)
z-centered 평균
3-member 등가중
앙상블 모델 구성

main :  amw4 s7070 · INT8 · 568MB

model_b :  구 recipe s909 · INT4 · 292MB

model_c :  amw4 s42 · INT4 · 292MB

최종 Macro F1 0.7977
17

<!-- Slide number: 12 -->
10. 검증 설계
내부에서 먼저 판정하고, 리더보드는 확인용으로만 썼다
세션 단위 3-fold OOF ↔ Public 캘리브레이션 오차 ±0.002 · 한 fold라도 음성이면 기각
검증 체계

① 세션 단위 3-fold OOF

② OOF ↔ Public 캘리브레이션 ±0.002

③ 교사 OOF 누수 차단 (fail-closed)

④ 로컬 KD는 fold-교사 누수로 +0.012 인플레

판정 기준

① 단일변수

② 내부 스크린 우선 → Public 슬롯

③ 판정 기준 → 한 fold라도 음성이면 기각

④ 배포 표면 재검증

⑤ KD는 매치드시드 Public
19

<!-- Slide number: 13 -->
11. 게이트를 폐기한 것도 검증 결과
내부 게이트가 Public과 역상관 ? 6번 시도해 6번 모두 역전됐다
게이트 시드폭 0.008 vs Public 0.0001 → 시드로는 점수가 오르지 않는다
게이트 ↔ Public 역상관

① 내부 게이트 우세 → Public 열세

② 6번 시도, 6번 모두 역전

③ 원인: 게이트 편차 = 암기 노이즈
→ 게이트 폐기도 검증 결과
시드：순수 노이즈

① 게이트 시드폭：0.008

② Public 시드폭：0.0001

→ 시드로는 점수 상승 불가

20

<!-- Slide number: 14 -->
12. 불가능한 축을 먼저 지웠다
음성 결과를 판독해 사전 기각 ? Public 슬롯을 아끼는 것이 곧 속도
내부 스크린으로 판정해 제출 없이 축을 닫았다
| 기각한 축 | 판정 근거 | 결론 |
| --- | --- | --- |
| 혼동클래스 (탐색4) | 동일 컨텍스트 93% 라벨 갈림 · Bayes 0.570 | aleatoric 한계 |
| 직렬화 변형 (v5/v6) | v5 ?0.0047 · 블렌드 오류중첩 85% | current\_v1 확정 |
| 인코더 라인 (XLM-R) | large 0.7733 < Qwen 0.7807 | 디코더로 전환 |
| 이질 멤버 (mBERT) | 배포 전 변형 음성 (?0.001~?0.005) | 축 완전 폐쇄 |
| 동계열 교사 (HCX-1.5B) | 홀드아웃 강했으나 Public ?0.0147 | 암기 교사 배제 |
| 거대 교사 (Qwen 9B) | train 일치율 0.943 → 이득 ±0.000 | 원핫 붕괴 |
| replay 조밀화 (A1) | 2-fold f0 +0.0008 / f1 ?0.0016 | 재생성 후 기각 |
| α/T 비율 탐색 | α0.7/T2 = α0.5/T3 동점 (0.789) | 무차별 영역 |
| int4 전면화 | argmax 94.5% (게이트 99.5%) | int8 유지 |
| test-time tta | 5-fold 시뮬 전 폴드 음성 | 기각 |
| 시드 리롤 | 2k↔20k 순위 완전 재셔플 = 노이즈 | best-of-N 전환 |
| 세션 체인 누수 | chainprobe = 기준선 완전 동점 | 누수 0 확정 |
21

<!-- Slide number: 15 -->
13. 속도 제약과 병목
forward 연산이 시간의 95% ? 길이 캡을 줄여도 시간은 안 줄었다
서버 로그로 시간을 회귀해 제출 전 예측 (v5 6:55 / m7 8:55, 토큰비 0.80 일치)
병목 규명

① forward 연산이 시간의 95%

② 동적 패딩 → 시간 = 실제 토큰 수

③ 길이 캡 축소 = 시간 절감 0
→  직렬화 자체 또는 모델을 축소하는 방향
시간 예산 산식

① 서버 시간을 제출 전에 예측
→ v5 6:55 / m7 8:55 = 토큰비 0.80 일치

② 단독 ≤ 0.6B급만 10분 통과

③ 0.8B 3-epoch = 시간 초과 (실측)
→ 시간초과 실패: 의도한 초기 2건 이후 0건
23

<!-- Slide number: 16 -->
14. 자체 양자화 코덱
1GB 제약을 코덱으로 풀었다 ? 다만 전면 int4는 성능이 무너졌다
int8 1132→568MB(argmax 99.6%) / 멤버만 int4 292MB → 3개 합 1005.6MB
자체 양자화 코덱

① int8 row-wise
→ 1132MB → 568MB · argmax 99.6%

② int4 group-128
→ 292MB

→ 0.5B × 3 = 1005.6MB (1GB 안)

제출 시간 문제 발견

① int4 전면화 시도
→ main도 int4 → argmax 94.5%로 열화

② 0.6B 열화 증가 문제
→ main은 int8 유지, 멤버만 int4

→ 용량 여유는 있으나 시간 제약으로 인한 실패

24

<!-- Slide number: 17 -->
15. 최종 성능
15일 · 제출 115회 · 최고점 갱신 24회
베이스라인 0.4358 → 최종 0.7977 (+0.362) · Public 8위

![](그림4.jpg)
27

<!-- Slide number: 18 -->
16. 정리
라벨을 다시 정의한 것이 알고리즘 선택을 전부 결정했다
발견 → 선택 → 검증의 세 축이 모든 개선에 인과 근거를 남겼다
문제를 다시 정의했다
WHY

라벨 = 확률적 정책 기록
텍스트로 도달 불가 (CMI?0)

교차 검증으로 입증
LLM 상호 79.8% vs 라벨 21%

→ 고치는 대신 압력 완화
그 정의가 알고리즘을 골랐다
HOW

교사 KD + Weak4 AM
consensus sieve · 조건부 α

budget 룰 12개 (OOF 통과분)
저마진 34.1%만 앙상블

→ 0.7852 → 0.7977
모든 경로에 근거를 남겼다
PROOF

OOF ↔ Public ±0.002
게이트 역상관 6전 6패 실증

시드 = 순수 노이즈 규명
불가능 축은 제출 전 기각

→ 운이 아니라 검증의 결과
28

<!-- Slide number: 19 -->
2026.08.11
29
