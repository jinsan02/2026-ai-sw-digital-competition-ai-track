# 07 — 팀원별 요청사항 (Slack 공유용)

> ✅ **07-15 DM 발송 완료** (준현·원주·태연 각자 요청 전달, 목요일 회신 요청).
> DM 검토 결과 이미 확보된 재료: 준현(base CLI·compile 서사·방법론 주의) / 원주(절단감사·CL 21건·sim/au·양자화세금 → `03` 반영) / 태연(TTA 강건성 probe → `03` 반영).
>
> 🎉 **07-15 밤 태연 회신: 14슬라이드 발표자료 초안(PPTX+PDF) 도착 → 팀 베이스 채택** (팀채널 공지 완료).
> 태연 수정 할당: 최종점수 0.7972→**0.79767**(amw4_7070m10)·패키지 512MB→1005.6MB·슬라이드9 마지막 이틀 서사(trioT→Weak4-AM, 준현 명세 대기)·1위갭 재계산. 편집 오너=태연(PPTX), PDF 변환 후 제출.
> 미결: 슬라이드 13 역할 크레딧 팀 합의 (팀채널 스레드), 포스터 모니터링(태연 유지).
>
> ✅ **07-15 밤 원주 회신: EDA 3장면 문안 전문 도착** → `08_원주_EDA문안.md` 보관+검증 완료 (정합 6건 확인,
> 확인요청 3건: 71.8% vs 60% 통일 / 41% 이중등장 / fig3 190k+ 자릿수). **그림 3장(PNG)은 미착 — 원주 업로드 대기.**
> 원주 리허설 07-19(일) 저녁 가능.

> 마감: **07.20(월) 10:00** 구글폼 (코드 + 발표 PDF). 오늘 07.15(화) → **실질 D-4 (주말 포함)**.
> 이 폴더 초안(00~06)을 베이스로 각자 파트를 채워주세요. 없는 정보는 제가 못 쓴 부분입니다.

---

## 🔴 준현 (모델 고도화 — 최우선, 코드 제출 블로커)

1. **최종 챔피언(amw4_7070m10) 완전 명세** — `01`·`05`의 ⚠️ 항목:
   - Weak4 action-margin KD 손실식 한 문단 + 정확한 학습 CLI
   - amhyb_m10("트리오 변경")과의 차이 (동점 0.7976673203인데 구성이 뭐가 다른지)
   - 최종팩 model_b/model_c 시드·변형 명세
   - margin 1.25→1.0 재축소가 amw4에서 이긴 이유 (해석 한 줄)
2. **학습 코드 패키징** — 그의 레포 기준: trioT teacher 로짓 추출 스크립트, amw4 학습 코드, Colab 노트북 원본. 상대경로·UTF-8 정리는 진산이 최종 검수.
3. Private 복원 서사: "이 코드+이 시드로 이 무게가 나온다" 학습 로그 원본 (07-14~15 lane C 로그).

## 🟡 원주 (데이터 분석)

1. **발표 슬라이드 2·10 보강**: EDA 핵심 3장면 — ① 클래스 분포/불균형(Weak4) ② 탐색4 aleatoric 증명(동일 컨텍스트 93% 라벨 갈림 — 본인 감사 트랙 A 결과 포함) ③ budget-zone 발견(→R1 룰 계열의 기원).
2. 절단 감사 결과 요약(배포 파이프라인 절단율 0.00%, p99=315 토큰) — "짧은 레코드가 오히려 err↑" 관찰 포함.
3. D-1 총정리 Canvas(F0BH30L8D1T) 내용 중 발표에 넣을 그림 후보 선별.

## 🟡 태연 (추론 코드·제출 관리)

1. **script.py 추론 파이프라인 설명 1페이지** (발표 슬라이드 5 소스): 직렬화→3모델 로드→저마진 라우팅→z-평균→룰 적용 순서, 시간 분해(모델별 forward 시간).
2. 제출 오류 이력·대응 정리 (`04` 오류 6건) — 본인 관리 관점에서 한 문단.
3. 포스터 양식 공개 모니터링 (7.30 마감, 평가 미포함이지만 필수 제출).

## 🟢 진산 (팀장 — 취합)

1. 00~07 초안 유지보수 + 팀원 파트 머지
2. 발표 PDF 조립 (md → 디자인) + 그래프 3종 제작 (`06` 체크리스트)
3. 구글폼 링크 공지 모니터링 + 제출
4. **전문가 심사 평가항목 공개 시** `06` 목차 재배열 (전 문서 중 최우선 트리거)
5. 학습 코드 최종 검수: 클린룸 `pip install` + UTF-8 + 상대경로

## 공통

- [ ] 발표자 선정 + 리허설 일정 (권장: 07.19 일요일 저녁 1회 + 본선 전주 1회)
- [ ] 각자 파트에서 "내가 한 것" 1문단씩 → 슬라이드 11(협업) 소스
- [ ] QnA 20문(`06`) 중 자기 파트 답변 검토·보강

---

### 우리가 이미 가진 것 (참고)

- 제출 로그 120건 전체 정리 (`04`)
- 기각축 12개 + 사고 사후분석 5건 (`03`)
- 챔피언 계열 base CLI·환경 매트릭스·출처 표 (`05`)
- 발표 구성안 + QnA 20문 (`06`)
- 준현 레포 커밋 히스토리 (레인 기록: relational-KD → action-margin → Weak4-AM champion)
- 진산 레포: 초기 파이프라인·verify_zip·탐색 분석팩 (07-04 시점)
